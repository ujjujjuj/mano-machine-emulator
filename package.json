{
  "name": "mano-machine-emulator",
  "version": "1.0.0",
  "license": "MIT",
  "source": "src/index.html",
  "scripts":{
    "dev":"parcel",
    "build:parcel": "parcel build ./src/index.html --public-url ./ --no-source-maps",
    "build": "npm run clean && npm run build:parcel",
    "clean": "rm -rf dist/*"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.7.0",
    "parcel-reporter-static-files-copy": "^1.4.0"
  }
}
